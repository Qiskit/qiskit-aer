---
upgrade:
  - |
    In Aer 0.8.0, the ``qobj`` parameter was replaced by ``circuits`` and ``schedules``
    in :class:`.AerSimulator` and :class:`.PulseSimulator` respectively.  The ``qobj``
    parameter for ``backend.run`` is now removed.
